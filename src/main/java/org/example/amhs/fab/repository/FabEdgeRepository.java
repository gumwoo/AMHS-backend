package org.example.amhs.fab.repository;

import org.example.amhs.fab.domain.FabEdge;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FabEdgeRepository extends JpaRepository<FabEdge, String> {
}
